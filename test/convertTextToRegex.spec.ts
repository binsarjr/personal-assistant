// import { describe } from 'node:test'
// import { convertTextToRegex } from '../src/utils'

// describe('Convert text To Regex', function () {
//   const texts = ['terima kasih']

//   const data = ['terima kash', 'trma']
//   data.map((text) => {
//     const regex = convertTextToRegex('terima kasih')
//     console.log(regex,text,regex.test(text))
//     // assert.equal(true, convertTextToRegex(text).test('terima kasih'))
//   })
// })

// import FuzzySet from 'fuzzyset'
// const fuzz = FuzzySet(['terima kasih', 'makasih'])

// const text = 'Terima kasih'

// console.log(fuzz.get(text, 0.8))
